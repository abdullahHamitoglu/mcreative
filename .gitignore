node_modules
.next
.claude
.env
*.tsbuildinfo
media
public/media
.DS_Store
npm-debug.log*
pnpm-debug.log*

# Generated by Payload (generate:importmap / generate:types). Committed rather than
# regenerated at build time — Dokploy's nixpacks build runs plain `next build` with no
# prebuild step, matching moumin-designer's working deployment setup.
